# 🎥 YouTube AI Comment Analyzer (n8n + Gemini + Chrome Extension)

Автоматизированная система для анализа комментариев YouTube в реальном времени. Инструмент позволяет получать суммаризацию мнений и оценку настроения прямо на странице видео через браузерное расширение.

---

## 🛠 Архитектура решения

Проект построен на интеграции трех независимых модулей:

1.  **Chrome Extension**: Легкий интерфейс (Frontend), который захватывает URL текущего видео и передает его в облако через Webhook.
2.  **n8n Workflow**: Ядро системы (Backend), выполняющее оркестрацию данных, фильтрацию авторов и обработку ошибок.
3.  **AI & Data Layer**: 
    * **Apify API**: Скрапинг данных с высокой скоростью.
    * **AlmemLLM**: Анализ текста с помощью казахстанской LLM.

---

## 🚀 Функциональные возможности

* **Smart Filtering**: Игнорирование ответов автора канала для получения объективной картины мнений зрителей.
* **Error Handling**: Обработка пустых данных и системных сбоев (например, отсутствие комментариев или ошибки API).
* **Secure Auth**: Использование системы Credentials в n8n для защиты API-ключей (ключи не хранятся в коде воркфлоу).

---

## 📋 Инструкция по развертыванию

### 1. Подготовка n8n
1.  Импортируйте файлы из папки `/n8n-workflow` в ваш экземпляр n8n.
2.  Создайте новые **Credentials** типа `Header Auth` для Apify:
    * **Name**: `Authorization`
    * **Value**: `Bearer YOUR_APIFY_TOKEN`.
3.  Создайте **Credentials** для OpenAI, где Base URL: https://llm.alem.ai/, API Key *: YOUR_API_KEY.
4.  В нодах `Post-url-for-comment-scraps` и `Get-comment-dataset` выберите созданные учетные данные (URL в нодах очищены от токенов в целях безопасности).

### 2. Установка расширения
1.  Откройте `chrome://extensions/` в браузере.
2.  Включите **Developer mode**.
3.  Нажмите **Load unpacked** и выберите папку `/chrome-extension`.
4.  В файле `popup.js` замените `WEBHOOK_URL` на адрес вашего Webhook из n8n.

---

## 🛡 Безопасность
Проект следует принципу **Zero Trust** в отношении секретов:
* API-токены вынесены в защищенное хранилище n8n.
* В экспортированных JSON-файлах отсутствуют приватные ключи.
* Реализована валидация входных данных для предотвращения пустых запросов к LLM.
